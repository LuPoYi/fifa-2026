---
name: middle-play
description: 「不會雙輸」middle 對沖玩法的完整工作流:設計組合、算注碼、背景監看盤中賠率、觸發警報。使用者說「這場想玩 middle」「不會雙輸的組合」「幫我盯補腿時機」「legging」時觸發。娛樂性質,SX Bet 世界盃專案用。
---

# Middle Play(中間注)工作流

## 名詞表(使用者指定,對話一律用這套)

| 英文 | 中文(用這個) | 別再用 |
|---|---|---|
| straight bet | 單注 | — |
| middle | 中間注 | — |
| leg | 第一注/第二注 | ~~腿~~ |
| legging in | 分段進場 | ~~補腿~~ |
| hedge | 對沖 | — |
| arbitrage | 套利(=無風險門檻達成) | — |
| push | 退款 | — |
| park the bus | 擺大巴(死守) | ~~鐵桶~~ |
| value bet | 價值注 | — |
| win by exactly N | 剛好贏 N 球 | ~~恰贏~~ |

SX **沒有**「總進球=N」「淨勝球=N」「波膽」現成盤(實測只有獨贏/1X2/讓分/大小/晉級)→ 這類玩法必須用中間注自組,等效賠率約打 97 折(付兩次 vig)。maker 掛單 0% fee,賽前可掛優於市價的單等成交(注意逆向選擇)。

使用者喜歡的下注結構:兩注的贏面**聯集蓋滿所有結果**(雙輸不可能),重疊區 = 雙贏爆賺。
偏好**窄重疊、雙贏大勝**型;單邊結果小虧或保本即可。娛樂性質、小額,誠實提醒 EV ≈ 打平(vig)。

## 核心數學(必背)

- **對等對沖注碼**:已持有 A(本金 S、賠率 oA),補 B 時 → **U = S × oA ÷ oB**(兩個單邊結果損益拉平)
- **無風險門檻**:`1/oA + 1/oB < 1` → 補後三種結果全 ≥ 0,重疊區純 bonus
  - 例:oA=2.27 → oB ≥ 1.79 即無風險
- 損益:雙贏 = S(oA−1) + U(oB−1);只中A = S(oA−1) − U;只中B = U(oB−1) − S

## 常用結構(驗證「聯集蓋滿」再下)

| 結構 | 兩腿 | 雙贏區 | 蓋滿原因 |
|---|---|---|---|
| 大小 middle | Over N + Under M(N<M) | 總分在 (N,M) | 總分不可能同時 <N 又 >M |
| 讓分 middle | A −x.5 + B +y.5(y>x) | 淨勝分在中間 | 同上 |
| 比分連動(盤中) | Over 2.5 + 落後方不贏 | 有球但落後方沒贏 | 落後方要贏必灌球 |
| 雙不輸 | A不輸 + B不輸 | 平手 | 雙輸 ⟺ 兩隊都贏(不可能) |

⚠️ 名詞:SX 的「A 不輸」= 網站的 **Not (對手)**。

## 工作流

1. **賽前**:用 `python3 sx.py odds <eventId> --all` 抓全盤口,依使用者看法挑結構,列
   「雙贏/單邊A/單邊B」損益表(格式:結果|例子比分|淨損益,標明 🔒雙輸不可能)。
2. **Legging(分腿)**:使用者常先單押 value 邊,等盤中另一腿賠率變好再補。
   給 cheat sheet:各賠率水位 → 補多少(U = S×oA÷oB)→ 三格損益。
3. **背景監看**:寫輪詢腳本(參考 `watch_under*.py` pattern,scratchpad 內)用
   `run_in_background` 跑,**觸發即 break 讓主程序收到通知**。監看事件:
   - 補腿賠率 ≥ 目標門檻(報:賠率、可吃量、建議注碼)
   - 賠率掉回(窗口關閉)
   - 每次進球(比分變 → 重算劇本)
   - 總分越線(某腿已死/已中)、終場(結算)
   - 輪詢間隔 ≥6s(`/orders` 限速 20/10s);log 稀疏(2-4 分鐘一行)
   - ⚠ 終場判定:live-scores 的 `currentPeriod` 實測回 **"Full time"**(不是 "Finished"),
     判斷用 `per in ("Finished","Full time")`;periodTime 5400 = 90 分鐘打完
   - ⚠ **結算範圍(官方查證)**:**大小分 / 勝平負 / 讓分 = 只算 90 分 + 傷停,不含延長賽/PK**
     (淘汰賽 1-1 到 90 分即結算為平手/該總分,之後延長賽不計);**「晉級 / 奪冠」盤才含延長 + PK**。
     → 看不了直播、想壓弱隊時,用**晉級盤**最省心(弱隊多了延長/PK 兩條活路,不必 90 分內解決)。
4. **警報時**:直接給「下多少 + 三格損益表」,提醒以使用者畫面當下價為準、看可吃量。

## 名詞:order book(訂單簿)

使用者指定一律用英文 **order book**(不用「書單/訂單簿」)。SX 是交易所,賠率由使用者掛單(maker)撮合,taker 去吃;order book 空/薄 = 沒對手可成交。

## Over/Under 開局時機 + leg 進套利(2026-07-07 驗證)

- **開局 0-0 → Over 賠率漲**:實測 Over 2.5 賽前 2.01 → 8 分 0-0 漲到 2.36。**機制成立,但不是 value**:漲幅≈反映進球機率下降(時間變少),EV 幾乎不變;且 ~25-30% 場次前 10 分就進球、反而讓 Over 變便宜。屬時機/波動偏好,非撿便宜。
- **leg 進 totals 套利**:先押 Under 2.5(開局 ~1.95),若續 0-0 → Over 2.5 漲到 ~2.3+,此時 `1/oUnder + 1/oOver < 1` 就成立(實測 1/1.95+1/2.36=0.937)→ 兩邊互補、back 兩邊鎖套利。**注意**:①要先承諾 Under、若早進球則套利不開、只剩一張還算公道的 Under(**那條分支不是「不輸」**);②in-play Over 這邊 order book 薄、鎖利額受量限制;③鎖利幅度小(~5-7%)。
- **反向也成立(本場實例)**:已持有 Over 2.5 @2.01,盤中對手先進球、Argentina 壓著追 → Under 2.5 漲到 2.71,`1/2.01+1/2.71=0.87<1` → 補 Under 即鎖穩賺。**通則:自己那腿吃得夠便宜時,對面漲到 `1/oA+1/oB<1` 就能 leg 成無風險鎖利**;補注碼 U = (自己那腿總回收) ÷ 對面賠率。

## 實戰教訓(2026-07-03 Swiss vs Algeria)

- 窗口開關很快:進球 → 補腿賠率跳高(1.20→1.55)→ 幾分鐘沒球就掉回(→1.35)。
  **到合理價就動,別貪無風險門檻**(等 1.79 常等到窗口關閉)。
- 第二顆球會重開窗口;比分每變一次,「還差幾球」的劇本就要重算。
- 盤中 order book 薄:報價時一併報「最佳價可吃量」,不足額要說。

## 誠實提醒(每次都講)

結構只決定賠付形狀,不會把負 EV 變正;這是娛樂/控波動玩法。
詳細理論見使用者個人 HackMD「運動博弈不會雙輸對沖玩法」。
